Modificare la configurazione in una root di progetto locale
impostare come edito di configurazione il blocco note di windows
git config --global core.editor "notepad"

aprire il file di configurazione git locale 
git config --local --edit
